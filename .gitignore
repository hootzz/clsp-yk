# --- data & private artifacts (never publish) ---
Data_files/
data/
**/data/watch/
*.jsonl
paper/

# --- credentials ---
.env
*.env
**/secrets*

# --- model weights (large / not redistributed) ---
*.pt
*.pkl
*.safetensors
weights/
**/papagei-foundation-model/

# --- runtime outputs ---
outputs/
reports/
work_model/
cache/
processed_*.csv
raw_*.csv
dump_*.txt

# --- python ---
**/__pycache__/
*.pyc
.venv/
venv*/

# --- internal working docs (NOT for the public repo) ---
# paper analysis, section rewrites, verification, KO drafts, planning notes.
# These stay local for authoring; they are not part of the published pipeline.
paper/
*_internal.md
_final_*.jsonl
_*_test.*

# --- local evidence copies (편의용, 공개 안 함) ---
results/_evidence/
